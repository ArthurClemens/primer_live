This directory stores failed test results.

Verify the results, then either:

1. Move files manually to `test/assertions`
2. Run the accept script:
   - `mix:accept`
   - `mix:accept subdir`
   - `mix:accept subdir subfile.html`
